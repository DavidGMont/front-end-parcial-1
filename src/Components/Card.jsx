import PropTypes from 'prop-types';

const Card = (props) => {
    return (
        <>
            <h2>Hola, soy una tarjeta 👋😀</h2>
        </>
    );
};

Card.propTypes = {};

export default Card;
